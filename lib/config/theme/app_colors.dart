import 'package:flutter/material.dart';

class AppColors {

  static const appPrimaryExtraDarkColor = Color(0xFF115e59);
  static const appPrimaryDarkColor = Color(0xFF0f766e);
  static const appPrimaryLightColor = Color(0xFF0d9488);

  static const appDarkNavyColor = Color(0xFF0F172A); // Dark Navy / Dark Slate
  static const appDarkNavy1Color = Color(0xFF15274E); // Dark Navy / Dark Slate

  static const appScaffoldBackgroundColor = Color(0xFFf5f5f8);

  static const appBlackColor = Colors.black;
  static const appWhiteColor = Colors.white;

  static const lightWhiteColor = Color(0xFFEDECEC);
  static const lightBlackColor = Color(0xFF1C1C1E);
  static const littleBlackColor = Color(0xFF0D0D10);

  static const blueColor = Colors.blue;
  static const violetColor = Color(0xFF8F2FF3);
  static const lightVioletColor = Color(0x96D9C2F8);

  static const cyanColor = Colors.cyan;

  static const darkGreenColor = Color(0xFF076501);
  static const lightGreenColor = Color(0xFFE7F8E5);

  static const greenColor = Colors.green;
  static const purpleColor = Colors.purple;
  static const deepPurpleColor = Colors.deepPurple;
  static const orangeColor = Colors.orange;
  static const deepOrangeColor = Colors.deepOrange;
  static const orangeAccentColor = Colors.orangeAccent;
  static const amberColor = Colors.amber;
  static const redColor = Colors.red;
  static const greyColor = Colors.grey;

  static const darkGreyColor = Color(0xFF6E6E6E);
  static const mediumGreyColor = Color(0xFFB0B0B0);
  static const lightGreyColor = Color(0xFFCCCCCC);

  static const textLightColor = Color(0xFF0D0D0D); // Text label
  static const textDarkColor = Color(0xFFFFFFFF); // Text label

  static const errorTextColor = Color(0xFFFF2600);
}